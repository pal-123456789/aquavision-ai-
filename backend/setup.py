from setuptools import setup, find_packages

setup(
    name="aquavision",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        # List your dependencies here or they'll be installed from requirements.txt
    ],
)